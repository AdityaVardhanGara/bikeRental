swagger: '2.0'
info:
  title: Sample API
  version: '1.0'
paths:
  /hello:
    get:
      summary: Returns a greeting message
      responses:
        '200':
          description: A greeting message
          schema:
            type: string
